This program receives three numbers from C and compares a with b. If a is greater than b, 
it compares a with c. If a is greater than c, it returns a. But if b is greater than a, it compares b with c. If b is greater than c, it returns b.
Otherwise, it returns c.
